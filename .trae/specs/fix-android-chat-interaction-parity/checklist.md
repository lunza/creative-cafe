# Checklist

## 头像查看
- [x] 点击 AI 头像/立绘弹出全屏查看器（当前立绘或头像原图）
- [x] 双指捏合缩放 1x–4x 平滑；双击在 1x/2.5x 间切换；放大后单指拖拽平移（双击/关闭实测通过；捏合 adb 无法模拟，代码审查通过留真机复核）
- [x] 单击遮罩 / 右上角关闭按钮 / 系统返回键均可关闭
- [x] 用户头像：persona 有头像时显示图片并可点击查看；无头像时文字圈 + 点击提示「当前人设未设置头像」

## 气泡样式（PC 基准）
- [x] 用户气泡圆角 18/18/4/18（右下小角 4），indigo→violet 渐变、白字
- [x] AI 气泡圆角 18/18/18/4（左下小角 4），半透明深色 + 细边框（亮色主题浅色玻璃底可读）
- [x] 内边距水平 16 / 垂直 12；文本 selectable 长按可复制
- [x] 名字行显示：用户名 / 角色名 + 情绪标签 + AI 序号徽章 #n（非图片消息计数）

## 卷回 / 重新生成
- [x] 用户气泡下方有「卷回到输入框」按钮；点击后消息截断（服务端 + 本地一致）、内容填入输入框、Snackbar「已卷回到输入框」
- [x] AI 气泡下方有「重新生成」按钮；完成后历史为 user + 新 AI 回复，无重复 user 消息
- [x] 复制按钮：AI 消息内容复制到剪贴板 + Snackbar 反馈
- [x] 流式期间卷回/重新生成禁用；空历史/仅问候语时卷回回退正常（列表剩问候语或空态）
- [x] rollback 接口异常路径返回结构化错误（MESSAGE_NOT_FOUND / NOT_USER_MESSAGE / CHARACTER_NOT_FOUND），存储不被破坏

## 辅助模式
- [x] 点击推荐选项 → 文本填入输入框（可编辑），不直接发送；Snackbar 提示「已填入输入框，可编辑后发送」

## 回归与响应式
- [x] 思考面板三态、图片气泡（生成/历史切换/全屏）、记忆表格、会话配置等既有功能无回归
- [x] 窄屏(<380) / 常规 / 横屏三档布局无错位；亮暗主题下新 UI 配色统一可读
- [x] 客户端 tsc 0 错误；assembleRelease 构建通过；APK 安装模拟器实测
- [x] 服务端 npm test 无回归

## 交付
- [x] 测试报告输出（测试用例、结果、修复前后对比）
- [x] docs/android-client.md / CHANGELOG.md / FIX_RECORDS.md 增量更新（本批缺陷重点标记）
