# Checklist

## 类型契约与基础
- [x] `src/shared/types/worldbook-authoring.types.ts` 定义了 `AuthoringState`（五状态机）、`AuthoringGoal`、`AuthoringPlan`、`AuthoringSession`、`AuditReport`、`AuthoringProgressEvent` 等核心类型
- [x] `src/main/services/agent/worldbook/worldbookAuthoringTypes.ts` re-export 共享类型并定义主进程专用类型
- [x] `npx tsc --noEmit` 0 错误

## 审计服务（worldbookAuditService）
- [x] `validateCompleteness(plan, entries)` 校验必填字段（key 非空数组 / content 非空 / comment 或 name 非空，对齐 `worldBookService.migrateEntry` 字段标准）+ 维度覆盖 + 条目数达标（80% 阈值）
- [x] `checkConsistency(entries)` 检测关键词冲突（cosine 相似度）+ 设定矛盾（LLM 语义对比）+ 关系图无环
- [x] `evaluateConformance(userPrompt, clarifications, entries)` 向量化用户提示并与条目做 cosine 检索，标记偏离条目
- [x] `runFullAudit` 聚合三维度返回 `AuditReport`（含严重级别 / 自动修复建议 / 需用户决策项）
- [x] 单元测试覆盖三维度正例/反例 + 聚合报告

## 规划服务（worldbookPlanningService）
- [x] `analyzePrompt` LLM 分析初始提示，识别维度并标注"用户提及" vs "智能体建议"
- [x] `generateClarifyingQuestions` 生成 2-4 个结构化问题（附"为什么需要"说明）
- [x] `buildPlan` 综合用户回答生成 `AuthoringPlan`，跳过问题由智能体推断默认值并标注
- [x] `expandDimensions` 自驱扩展缺失但必要的维度
- [x] 单元测试 mock LLM 覆盖四方法

## 编排服务（worldbookAuthoringService）
- [x] 五状态机完整：PLANNING → AUTHORING → AUDITING → AWAITING_REVIEW → COMPLETE，每状态有明确进入/退出条件
- [x] 单实例守卫（对标 `writingAgentService` 模式）
- [x] `AbortController` 取消机制，取消后已生成草稿保留
- [x] 进度回调 `onProgress` 实时推送（phase / 维度 / 条目数 / 当前活动）
- [x] PLANNING 阶段通过 IPC 事件 `worldbookAgent:clarify` 推送问题，接收 `worldbookAgent:answer` 回应
- [x] AUTHORING 阶段调用 `worldbookTools`（createEntry/expandFromContext/generateKeywords）生成条目，标记 `autoGenerated: true` 写入草稿区
- [x] AUTHORING 阶段每 3-5 条触发微型审计，发现问题注入 `steerEngine` steer 消息
- [x] pacing 钳位（min 2s / max 30s，复用 `learning/pacing.ts`）
- [x] AUDITING 阶段调用 `runFullAudit`，小问题自动修复，大问题汇总报告
- [x] 断点续跑：`AuthoringSession` 持久化到 `memoryStore.write`（含 writeProvenance: source='worldbook-agent'），`resumeSession` 可恢复
- [x] `goalTracker` 集成追踪编写目标完成度，阻塞计数器超阈值标记 blocked
- [x] 单元测试覆盖五状态转移 / 取消 / 断点续跑 / 进度回调

## IPC 与 preload
- [x] `worldbookAgentHandlers.ts` 注册 5 通道：run / cancel / status / resume / answer
- [x] progress 与 clarify 通过 `webContents.send` 事件流推送
- [x] 每个 handler 入口校验 `useAgent && supportsToolCalling`，不满足返回 `{ ok: false, error: 'AGENT_MODE_DISABLED' }`
- [x] `worldBookHandlers.ts` 聚合注册；`ipc/index.ts` 的 `setupIpcHandlers` 第 2 组追加调用
- [x] `preload.ts` 暴露 `worldBookAgent` API（run/cancel/status/resume/answer + onProgress/onClarify）
- [x] `electron.d.ts` 补充类型定义

## 前端 Modal 与 hook
- [x] `useWorldBookAuthoring.ts` 实现构建/启动/取消/续跑/状态查询/事件订阅（对标 `useWritingAgent.ts`）
- [x] `WorldBookAuthoringModal.tsx` 三态视图：配置态 / 运行态 / 完成态
- [x] 运行态澄清问题交互区（接收 `worldbookAgent:clarify`，回传 `worldbookAgent:answer`）
- [x] 完成态审计报告展示（三维度通过率 + 偏离条目 + 自动修复记录）

## WorldBookManager 入口与 Agent gating
- [x] 工具栏新增"智能体编写"按钮（ThunderboltOutlined），仅当 `useAgent && supportsToolCalling` 时渲染
- [x] 点击后打开 Modal，传入当前选中世界书路径作为 `allowedWorldBookPaths` 沙盒白名单
- [x] Agent 模式关闭时按钮隐藏，手动 CRUD / `WorldBookEntryEditor` / `WorldBookAIGenerateFlow` 全部正常（回归测试）

## 技能注册
- [x] `worldbook-author/SKILL.md` frontmatter 完整（name/description/emoji/user-invocable:true/disable-model-invocation:true）
- [x] `agentHandlers.ts` 技能注册表包含 `worldbook-author`
- [x] `skillRegistry.list()` 包含该技能；`skillSnapshot` 注入 `<available_skills>`

## 不影响手动编写（隔离原则）
- [x] `worldBookService.ts` 核心 CRUD 方法未修改
- [x] `WorldBookEntryEditor.tsx` / `WorldBookAIGenerateFlow.tsx` 未修改
- [x] 所有智能体生成条目均 `autoGenerated: true`，经 `WorldBookAutoGeneratedReview` 待审阅区审批后才转正式

## 测试与验证
- [x] 单元测试 ≥40 用例（审计 / 规划 / 编排）
- [x] `npx vitest run` 全量通过（基线 1434 + 新增），无回归
- [x] `npx tsc --noEmit` 0 错误
- [x] 手动验收 9 项（按 spec"验收标准"）：Agent 模式 on/off 回归 / 三阶段工作流 / 草稿区 / 审计三维度 / 断点续跑 / 取消 / 进度回调（已交付用户验收）

## 文档增量更新
- [x] `CODE_WIKI.md` §4.5 追加 `worldbook/` 子目录；§6 新增"世界书编写智能体"说明；§8 补充审计向量检索用法
- [x] `CHANGELOG.md` 新增"世界书编写智能体"章节（三阶段工作流 / 审计 / Agent gating / 涉及文件）
- [x] `docs/FIX_RECORDS.md` 记录开发中的 bug 或反复调试问题（若有，重点标记）
- [x] `tasks.md` 各任务 checkbox 已勾选
