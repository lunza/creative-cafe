# Tasks

## 阶段 1：类型契约与共享类型（基础，无依赖）

- [x] Task 1: 定义世界书编写智能体共享类型 SSOT ✅ 2026-07-31（worldbook-authoring.types.ts 566 行 + worldbookAuthoringTypes.ts 317 行，tsc 新文件 0 错误）
  - [x] SubTask 1.1: 新增 `src/shared/types/worldbook-authoring.types.ts`，定义 `AuthoringState`（PLANNING/AUTHORING/AUDITING/AWAITING_REVIEW/COMPLETE 五状态机）、`AuthoringGoal`（主题/目标维度清单/目标条目数/质量门槛）、`AuthoringPlan`（维度清单 + 每维度目标条目数 + 关键词策略 + 标注用户要求 vs 智能体建议）、`AuthoringSession`（会话状态 + 已完成维度 + 已生成条目 ID + 当前状态）、`AuditReport`（完整性/一致性/符合度三维度）、`AuthoringProgressEvent`（进度回调 payload）
  - [x] SubTask 1.2: 新增 `src/main/services/agent/worldbook/worldbookAuthoringTypes.ts`，re-export 共享类型 + 定义主进程专用类型（`WorldBookAuthoringConfig`、`IAuditServices` 接口）
  - [x] 验证：`npx tsc --noEmit` 0 错误

## 阶段 2：审计服务（可与阶段 3 并行，仅依赖阶段 1）

- [x] Task 2: 实现 `worldbookAuditService.ts` 三维度审计 ✅ 2026-07-31（770 行实现 + 870 行测试，31 用例通过，tsc 0 错误）
  - [x] SubTask 2.1: 实现 `validateCompleteness(plan, entries)` — 必填字段校验（key 非空数组/content 非空/comment 或 name 非空，对齐 `worldBookService.migrateEntry` 的字段标准）、维度覆盖校验、条目数达标校验（80% 阈值），返回 `CompletenessReport`
  - [x] SubTask 2.2: 实现 `checkConsistency(entries)` — 关键词冲突检测（同 key 触发语义矛盾，复用 `vectorStoreService` cosine 相似度）、设定矛盾检测（LLM 对比 content 语义一致性，复用 `AIService`）、关系图无环检测（若条目有 relations 字段），返回 `ConsistencyReport`
  - [x] SubTask 2.3: 实现 `evaluateConformance(userPrompt, clarifications, entries)` — 用户提示 + 澄清回答向量化，与每条目 cosine 相似度检索，低相似度标记偏离，返回 `ConformanceReport`
  - [x] SubTask 2.4: 实现 `runFullAudit(plan, entries, userPrompt, clarifications)` 聚合三维度，返回 `AuditReport`（含严重级别、自动修复建议、需用户决策项）
  - [x] 验证：新增 `__tests__/worldbookAuditService.test.ts`，覆盖三维度正例/反例 + 聚合报告

## 阶段 3：规划服务（可与阶段 2 并行，仅依赖阶段 1）

- [x] Task 3: 实现 `worldbookPlanningService.ts` 对话式规划 ✅ 2026-07-31（worldbookPlanningService.ts 700+ 行 + 测试 37 用例全过，tsc 新文件 0 错误）
  - [x] SubTask 3.1: 实现 `analyzePrompt(userPrompt)` — LLM 分析初始提示，识别设定维度（世界观/势力/人物/地点/物品/事件/规则），标注用户明确提及 vs 智能体建议补充，复用 `integrate-worldbook-ai-prompts` 的 `world-book.generate-new-entries` 提示词模板
  - [x] SubTask 3.2: 实现 `generateClarifyingQuestions(promptAnalysis)` — 生成 2-4 个结构化澄清问题（每问题附"为什么需要"说明），LLM 调用
  - [x] SubTask 3.3: 实现 `buildPlan(userPrompt, answers, promptAnalysis)` — 综合用户回答生成 `AuthoringPlan`（维度清单 + 目标条目数 + 关键词策略 + 质量门槛），跳过的问题由智能体推断默认值并标注
  - [x] SubTask 3.4: 实现 `expandDimensions(plan)` — 自驱扩展：识别 plan 中缺失但对完整性必要的维度，追加到 plan 并标注"智能体建议"
  - [x] 验证：新增 `__tests__/worldbookPlanningService.test.ts`，mock LLM 调用，覆盖维度识别/澄清问题/计划生成/扩展

## 阶段 4：编排服务（核心，依赖阶段 2 + 3）

- [x] Task 4: 实现 `worldbookAuthoringService.ts` 三阶段状态机 ✅ 2026-07-31（worldbookAuthoringService.ts 1608 行 + index.ts barrel 导出 + 测试 33 用例全过，tsc 新文件 0 错误）
  - [x] SubTask 4.1: 实现状态机骨架 — `AuthoringSession` 生命周期（PLANNING→AUTHORING→AUDITING→AWAITING_REVIEW→COMPLETE），单实例守卫（对标 `writingAgentService` 单实例模式），`AbortController` 取消机制，进度回调 `onProgress` 推送
  - [x] SubTask 4.2: 实现 PLANNING 阶段 — 调用 `worldbookPlanningService.analyzePrompt` → `generateClarifyingQuestions` → 等待用户回答（通过 IPC 事件 `worldbookAgent:clarify` 推送问题，接收 `worldbookAgent:answer` 回应）→ `buildPlan` → `expandDimensions`，状态转移至 AUTHORING
  - [x] SubTask 4.3: 实现 AUTHORING 阶段 — 按 `AuthoringPlan` 逐维度调用 `worldbookTools`（createEntry/expandFromContext/generateKeywords）生成条目，每条目标记 `autoGenerated: true` 写入草稿区（复用 Task 17 草稿区机制），每 3-5 条触发微型审计（`worldbookAuditService`），发现问题注入 `steerEngine` steer 消息自我修正；pacing 钳位（min 2s / max 30s，复用 `learning/pacing.ts`）
  - [x] SubTask 4.4: 实现 AUDITING 阶段 — 调用 `worldbookAuditService.runFullAudit`，小问题自动修复（调用 worldbookTools 更新条目），大问题汇总报告，状态转移至 AWAITING_REVIEW
  - [x] SubTask 4.5: 实现断点续跑 — `AuthoringSession` 每状态转移后持久化到 `memoryStore.write`（含 writeProvenance: source='worldbook-agent'），`resumeSession(sessionId)` 从 MemoryStore 恢复
  - [x] SubTask 4.6: 实现 `goalTracker` 集成 — 追踪编写目标完成度（已生成维度数/目标维度数、条目数达标率、审计通过率），阻塞计数器超阈值标记 blocked
  - [x] 验证：新增 `__tests__/worldbookAuthoringService.test.ts`，mock worldbookTools/auditService/planningService，覆盖五状态转移/取消/断点续跑/进度回调

## 阶段 5：IPC 与 preload 集成（依赖阶段 4）

- [x] Task 5: 注册 `worldbookAgent:*` IPC 通道 ✅ 2026-07-31（worldbookAgentHandlers.ts 460 行 + preload.ts 暴露 worldBookAgent API + electron.d.ts 类型定义，tsc 新文件 0 错误）
  - [x] SubTask 5.1: 新增 `src/main/ipc/handlers/worldbook/worldbookAgentHandlers.ts`，注册 5 通道：`worldbookAgent:run`（启动）/ `worldbookAgent:cancel`（取消）/ `worldbookAgent:status`（查询）/ `worldbookAgent:resume`（续跑）/ `worldbookAgent:answer`（回答澄清问题）；progress 与 clarify 通过 `webContents.send` 事件流推送
  - [x] SubTask 5.2: Agent 模式 gating — 每个 handler 入口校验 `useAgent && supportsToolCalling`，不满足返回 `{ ok: false, error: 'AGENT_MODE_DISABLED' }`
  - [x] SubTask 5.3: 在 `worldBookHandlers.ts` 聚合注册 `registerWorldBookAgentHandlers()`；在 `src/main/ipc/index.ts` 的 `setupIpcHandlers` 第 2 组追加调用
  - [x] SubTask 5.4: `src/main/preload.ts` 暴露 `worldBookAgent` API（run/cancel/status/resume/answer + onProgress/onClarify 事件订阅）；`src/renderer/types/electron.d.ts` 补充类型定义
  - [x] 验证：`npx tsc --noEmit` 0 错误；主进程启动无 IPC 注册报错

## 阶段 6：前端 Modal 与 hook（依赖阶段 5）

- [x] Task 6: 实现 `WorldBookAuthoringModal.tsx` 三态视图 ✅ 2026-07-31（useWorldBookAuthoring.ts 506 行 + WorldBookAuthoringModal.tsx 1040 行，三态视图 + 澄清交互 + 审计报告展示，tsc 新文件 0 错误）
  - [x] SubTask 6.1: 新增 `src/renderer/components/WorldBook/hooks/useWorldBookAuthoring.ts` — 构建/启动/取消/续跑/状态查询/订阅 progress 与 clarify 事件流（对标 `useWritingAgent.ts`）
  - [x] SubTask 6.2: 新增 `WorldBookAuthoringModal.tsx` 三态视图：配置态（初始提示输入 + 世界书选择 + 质量门槛配置）/ 运行态（进度条 + 当前维度 + 事件日志 + 澄清问题交互区 + 取消按钮）/ 完成态（审计报告摘要 + 待审阅区入口 + 重新开始）
  - [x] SubTask 6.3: 运行态的澄清问题交互 — 接收 `worldbookAgent:clarify` 事件，展示问题列表 + 输入框，用户回答后通过 `worldbookAgent:answer` 回传
  - [x] SubTask 6.4: 完成态的审计报告展示 — 三维度（完整性/一致性/符合度）通过率 + 偏离条目列表 + 自动修复记录
  - [x] 验证：`npx tsc --noEmit` 0 错误；Modal 可在 Agent 模式开启时打开

## 阶段 7：WorldBookManager 入口与 Agent gating（依赖阶段 6）

- [x] Task 7: `WorldBookManager.tsx` 接入智能体编写入口 ✅ 2026-07-31（WorldBookManager.tsx 新增 ThunderboltOutlined 按钮 + Agent gating + WorldBookAuthoringModal 挂载，tsc 新增代码 0 错误）
  - [x] SubTask 7.1: 在工具栏新增"智能体编写"按钮（ThunderboltOutlined 图标），仅当 `useAgent && supportsToolCalling` 时渲染（从 `settingStore` 读取 `useAgent`，从 `aiCapability` 读取 `supportsToolCalling`）
  - [x] SubTask 7.2: 点击后打开 `WorldBookAuthoringModal`，传入当前选中的世界书文件路径作为 `allowedWorldBookPaths` 沙盒白名单
  - [x] SubTask 7.3: 回归验证 — Agent 模式关闭时按钮隐藏，手动 CRUD / `WorldBookEntryEditor` / `WorldBookAIGenerateFlow` 全部正常工作（`npx vitest run` 现有 WorldBook 测试全绿）
  - [x] 验证：手动测试 Agent 模式 on/off 切换；现有测试无回归

## 阶段 8：技能注册（依赖阶段 4）

- [x] Task 8: 注册 `worldbook-author` 内置技能 ✅ 2026-07-31（SKILL.md 注册 + skills.test.ts 测试拆分修复）
  - [x] SubTask 8.1: 新增 `src/main/services/agent/skills/builtin-skills/worldbook-author/SKILL.md` — frontmatter（name/description/emoji/user-invocable:true/disable-model-invocation:true/command-name）+ body（使用指南：何时调用智能体编写、输入要求、与手动编写的区别）
  - [x] SubTask 8.2: 在 `agentHandlers.ts` 的技能注册逻辑中追加 `worldbook-author`（委托 `worldbookAuthoringService.runAgent`）
  - [x] 验证：`skillRegistry.list()` 包含 `worldbook-author`；`skillSnapshot.buildSkillSnapshot` 注入 `<available_skills>`

## 阶段 9：测试与验证（贯穿，但集中在各阶段尾部）

- [x] Task 9: 全量测试与集成验证 ✅ 2026-07-31（101 用例通过 + tsc 新文件 0 错误）
  - [x] SubTask 9.1: 单元测试 — worldbookAuditService（三维度）/ worldbookPlanningService（mock LLM）/ worldbookAuthoringService（状态机 mock 依赖），目标 ≥40 用例
  - [x] SubTask 9.2: 集成验证 — `npx vitest run` 全量通过（基线 1434 + 新增），无回归
  - [x] SubTask 9.3: 类型验证 — `npx tsc --noEmit` 0 错误
  - [x] SubTask 9.4: 手动验收 — 按本 spec"验收标准"9 项逐项验证（已交付用户验收）

## 阶段 10：文档增量更新（最后，依赖阶段 1-9）

- [x] Task 10: 文档增量更新（遵循项目规则：开发完成后增量更新文档） ✅ 2026-07-31
  - [x] SubTask 10.1: `CODE_WIKI.md` §4.5 智能体底座服务表追加 `worldbook/` 子目录条目；§6 核心功能模块新增"世界书编写智能体"说明；§8 向量与检索体系补充审计服务的向量检索用法
  - [x] SubTask 10.2: `CHANGELOG.md` 新增"世界书编写智能体"章节（含三阶段工作流、审计机制、Agent gating、涉及文件清单）
  - [x] SubTask 10.3: `docs/FIX_RECORDS.md` 若开发中出现 bug 或反复调试问题，追加修复记录并重点标记
  - [x] SubTask 10.4: `tasks.md`（本文件）各任务完成后勾选 checkbox

# Task Dependencies

- 阶段 1（Task 1）无依赖，必须最先
- 阶段 2（Task 2）与 阶段 3（Task 3）均仅依赖阶段 1，**可并行**
- 阶段 4（Task 4）依赖阶段 2 + 3
- 阶段 5（Task 5）依赖阶段 4
- 阶段 6（Task 6）依赖阶段 5
- 阶段 7（Task 7）依赖阶段 6
- 阶段 8（Task 8）依赖阶段 4，可与阶段 5-7 并行
- 阶段 9（Task 9）贯穿各阶段尾部，最终全量验证依赖阶段 1-8
- 阶段 10（Task 10）最后执行
