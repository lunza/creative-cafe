# Checklist

- [ ] 保存设置时不再生成 `config.json`
- [ ] 初始化时不再创建 `config/` 文件夹
- [ ] 元数据（version、lastUpdated）正常存储到 `settings.json` 中
- [ ] `settings.json` 正常读写
- [ ] CONFIG 模块的 `get/set` 操作正常工作
- [ ] 应用核心功能验证通过