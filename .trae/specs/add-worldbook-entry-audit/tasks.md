# Tasks

- [x] Task 1: 新增审核提示词模板
  - [x] SubTask 1.1: 在 `src/main/services/promptTemplateService.ts` 中注册 `world-book.audit-content` 模板，包含四维度审核指令（内容正确性、主题一致性、格式规范性、语言表达），接受 `{{audit_requirements}}` 变量，指示 AI 以 JSON 格式返回 `{ passed, suggestions, revisedText }`
  - [x] SubTask 1.2: 在模板的 moduleId 映射表中注册（参照 `world-book.polish-content` 的注册方式）

- [x] Task 2: 新增审核状态管理
  - [x] SubTask 2.1: 在 `src/renderer/components/WorldBook/hooks/useWorldBookFormState.ts` 中新增审核相关状态：`auditingField`、`isAuditingAll`、`auditRequirements`、`isAuditModalOpen`、`currentAuditField`、`currentAuditText`、`auditAllRequirements`、`isAuditAllModalOpen`、`auditResult`（`{ passed: boolean; suggestions: string; revisedText: string } | null`）、`isAuditResultModalOpen`
  - [x] SubTask 2.2: 将新增状态加入 `UseWorldBookFormStateReturn` 返回对象

- [x] Task 3: 实现审核核心逻辑
  - [x] SubTask 3.1: 在 `src/renderer/components/WorldBook/hooks/useWorldBookAIOperations.ts` 中新增 `auditText` helper 函数，参照 `polishText` 的请求构建逻辑（URL 构建、请求体、API Key 传输、AI 请求发送），但使用 `world-book.audit-content` 模板，并使用 `parseAIJsonResponse` 解析返回的 JSON 为 `{ passed, suggestions, revisedText }`
  - [x] SubTask 3.2: 实现 `handleAudit(field: string)` — 校验引擎配置、设置 `currentAuditField` / `currentAuditText`、打开审核要求输入 Modal
  - [x] SubTask 3.3: 实现 `performAudit()` — 执行单字段审核，设置 `auditingField`，调用 `auditText`，将结果存入 `auditResult`，打开审核结果 Modal
  - [x] SubTask 3.4: 实现 `handleAuditAll()` — 校验选中条目和引擎配置，打开一键审核 Modal
  - [x] SubTask 3.5: 实现 `performAuditAll()` — 逐条审核选中条目的 content 字段，每条审核完成后弹出结果 Modal，用户决策后（采用/关闭）继续下一条，采用时更新 worldBookContent 并写入文件，支持 `isProcessingRef` 中断
  - [x] SubTask 3.6: 实现 `applyAuditResult()` — 将 `auditResult.revisedText` 替换到对应字段（单字段更新 formValues，批量更新 worldBookContent + 写文件）
  - [x] SubTask 3.7: 实现 `reAudit()` — 关闭结果 Modal，以原始文本重新发起审核
  - [x] SubTask 3.8: 在 hook 返回对象中导出所有审核函数

- [x] Task 4: 新建审核 Modal 组件
  - [x] SubTask 4.1: 新建 `src/renderer/components/WorldBook/WorldBookAuditModal.tsx`，包含三个 Modal：单字段审核要求输入 Modal、一键审核要求输入 Modal、审核结果展示 Modal
  - [x] SubTask 4.2: 审核要求输入 Modal 参照 `WorldBookPolishModal.tsx` 结构（TextArea 输入审核要求 + 取消/开始审核按钮 + 中断按钮）
  - [x] SubTask 4.3: 审核结果 Modal 展示：通过/不通过状态 Tag（绿色/红色）、修改建议文本区域、修改后文本预览区域，底部按钮："采用审核文本"（primary）、"重新审核"、"关闭"

- [x] Task 5: 编辑器添加单字段审核按钮
  - [x] SubTask 5.1: 在 `WorldBookEntryEditor.tsx` 的 Props 接口新增 `onAudit: (field: string) => void`
  - [x] SubTask 5.2: 在注释字段的 AI 按钮组（"AI翻译""AI润色"旁）添加"AI审核"按钮，`auditingField === 'comment'` 时显示"中断审核"
  - [x] SubTask 5.3: 在内容字段的 AI 按钮组同上添加"AI审核"按钮

- [x] Task 6: 批量操作面板添加审核按钮
  - [x] SubTask 6.1: 在 `WorldBookEntryTable.tsx` 的 Props 接口新增 `onAuditAll: () => void`
  - [x] SubTask 6.2: 在 `modalFooter` 中"一键润色选中条目"按钮旁添加"一键审核选中条目"按钮，显示选中数量，进行中显示"中断审核"，disabled 条件包含其他 AI 操作互斥状态
  - [x] SubTask 6.3: 在 useMemo 依赖数组和 disabled 互斥条件中加入 `isAuditingAll`

- [x] Task 7: 编排层接入审核
  - [x] SubTask 7.1: 在 `WorldBookManager.tsx` 中从 `formState` 解构审核相关状态
  - [x] SubTask 7.2: 向 `WorldBookEntryEditor` 传递 `onAudit={aiOps.handleAudit}`
  - [x] SubTask 7.3: 向 `WorldBookEntryTable` 传递 `onAuditAll={aiOps.handleAuditAll}`
  - [x] SubTask 7.4: 渲染 `WorldBookAuditModal` 组件，传递所有审核相关 props（要求输入 Modal + 结果 Modal）
  - [x] SubTask 7.5: 在 `modalFooter` 各 AI 按钮的 disabled 条件中加入 `isAuditingAll` 互斥

- [x] Task 8: 新生成内容审核入口
  - [x] SubTask 8.1: 在 `WorldBookAIGenerateFlow.tsx` 的新生成条目列表中，为每个条目添加"审核"按钮，点击后调用审核流程
  - [x] SubTask 8.2: 在 `WorldBookAutoGeneratedReview.tsx` 待审阅区条目中添加"审核"按钮

- [x] Task 9: 更新技术文档
  - [x] SubTask 9.1: 在 `.trae/documents/技术文档.md` 中增量更新世界书审核功能相关内容

# Task Dependencies
- [Task 2] depends on [Task 1]（状态中需要引用审核结果类型）
- [Task 3] depends on [Task 1] and [Task 2]（需要模板和状态）
- [Task 4] depends on [Task 2]（需要状态类型）
- [Task 5] depends on [Task 2]（需要 formState 中的审核状态）
- [Task 6] depends on [Task 2]
- [Task 7] depends on [Task 3], [Task 4], [Task 5], [Task 6]
- [Task 8] depends on [Task 3], [Task 7]
- [Task 9] depends on all previous tasks
