# Checklist

- [x] `world-book.audit-content` 提示词模板已在 `promptTemplateService.ts` 中注册，包含四维度审核指令和 JSON 输出格式要求
- [x] `world-book.audit-content` 模板接受 `{{audit_requirements}}` 变量
- [x] `useWorldBookFormState.ts` 中已新增全部审核状态（`auditingField`、`isAuditingAll`、`auditRequirements`、`isAuditModalOpen`、`currentAuditField`、`currentAuditText`、`auditAllRequirements`、`isAuditAllModalOpen`、`auditResult`、`isAuditResultModalOpen`）
- [x] `auditText` helper 已实现，参照 `polishText` 的请求构建逻辑，使用 `world-book.audit-content` 模板，并用 `parseAIJsonResponse` 解析 JSON 响应
- [x] `handleAudit` 函数已实现，校验引擎配置后打开审核要求输入 Modal
- [x] `performAudit` 函数已实现，调用 `auditText` 后将结果存入 `auditResult` 并打开结果 Modal
- [x] `handleAuditAll` 函数已实现，校验选中条目后打开一键审核 Modal
- [x] `performAuditAll` 函数已实现，逐条审核选中条目 content，每条弹出结果 Modal 供用户决策，支持中断
- [x] `applyAuditResult` 函数已实现，单字段更新 formValues，批量更新 worldBookContent + 写文件
- [x] `reAudit` 函数已实现，关闭结果 Modal 后以原始文本重新发起审核
- [x] 所有审核函数已在 hook 返回对象中导出
- [x] `WorldBookAuditModal.tsx` 已创建，包含单字段审核要求 Modal、一键审核要求 Modal、审核结果 Modal
- [x] 审核结果 Modal 展示通过/不通过状态 Tag（绿色/红色）、修改建议、修改后文本预览
- [x] 审核结果 Modal 提供"采用审核文本"、"重新审核"、"关闭"三个操作按钮
- [x] `WorldBookEntryEditor.tsx` 注释和内容字段工具栏已添加"AI审核"按钮，与"AI翻译""AI润色"并排
- [x] 审核进行中时按钮显示"中断审核"（danger 样式）
- [x] `WorldBookEntryTable.tsx` 批量操作面板已添加"一键审核选中条目"按钮，显示选中数量
- [x] 批量审核按钮与其他 AI 操作互斥（disabled 条件正确）
- [x] `WorldBookManager.tsx` 编排层已接入审核 props 传递和 `WorldBookAuditModal` 渲染
- [x] 所有 AI 操作按钮的 disabled 互斥条件已加入 `isAuditingAll` / `auditingField`
- [x] `WorldBookAIGenerateFlow.tsx` 新生成条目列表已添加"审核"按钮
- [x] `WorldBookAutoGeneratedReview.tsx` 待审阅区已添加"审核"按钮
- [x] AI 参数（temperature、max_tokens、model 等）从系统设置动态获取，未设置硬编码默认值
- [x] 技术文档 `.trae/documents/技术文档.md` 已增量更新审核功能内容
