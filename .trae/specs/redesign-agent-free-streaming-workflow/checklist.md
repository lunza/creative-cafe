# Checklist

## SKILL.md 更新

- [x] `worldbook-author` SKILL.md 移除三阶段工作流描述
- [x] SKILL.md 新增自由流式工作模式描述
- [x] SKILL.md 新增数据入库格式规范（条目结构、autoGenerated 标记、worldBookPath 传递、工具调用指引）

## handleWriteWorldbook 重写

- [x] `handleWriteWorldbook` 不再调用 `worldBookAgent.run` / `worldBookAgent.onProgress` / `worldBookAgent.answer`
- [x] `handleWriteWorldbook` 构建 systemPrompt 含技能格式规范 + 世界书路径上下文
- [x] `handleWriteWorldbook` 通过 `agent.run` 流式路径发送编写请求
- [x] `handleWriteWorldbook` 返回 void（不生成最终文本）
- [x] `sendMessage` 中 `/编写` 分支不调用 `appendAssistantMessage(resultContent)`

## 进度面板移除

- [x] `buildAuthoringProgressPanel` 函数及其常量已删除
- [x] `_progressPanelId` 使用已移除
- [x] `worldbookWriting` 状态及其使用已删除
- [x] `progressEvents` / `thoughtStepsAccum` / `onProgress` 订阅已删除
- [x] 残留会话清理逻辑已删除

## 逐项提问弹窗移除

- [x] `clarifyQuestions` / `currentClarifyIndex` / `pendingAnswers` / `awaitingSessionId` 状态已删除
- [x] `handleClarifyAnswer` 函数已删除
- [x] `clarifyState` 计算和导出已删除
- [x] `planning_clarifying` 事件检测逻辑已删除
- [x] `AgentDialogueModal.tsx` 中 `AgentQuestionModal` import 和渲染已移除
- [x] 输入框 `clarifyState.currentQuestion != null` 禁用条件已移除

## Loading 指示器和截断移除

- [x] `AgentDialogueModal.tsx` 中进度面板 Loading 指示器渲染已移除
- [x] `worldbookWriting` 解构已移除
- [x] 所有进度面板相关的 `.slice(0, N)` 截断逻辑已删除

## 流式输出体验

- [x] `/编写` 指令执行时 token 实时推送到对话框
- [x] 智能体思考过程和生成内容完整展示（无截断）
- [x] 智能体提问直接在对话框中展示（无弹窗）
- [x] 用户可直接在输入框回复（非 streaming 时输入框可用）
- [x] 无遮挡元素（弹窗、遮罩层、进度面板覆盖）

## 验证

- [x] `GetDiagnostics` 所有修改文件 0 新增错误
- [x] `CODE_WIKI.md` 已更新记录本次重构
