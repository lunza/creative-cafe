# Checklist

## 重构 buildPolishInputSystemPrompt 函数（`PromptBuilder.ts`）

### 改动 1：开头任务定义强化
- [x] 开头任务定义从"你是文本润色器，需要基于对话上下文优化用户 **${userName}** 的草稿文本。"改为"你是文本润色器，需要优化用户 **${userName}** 的草稿文本。**禁止生成对话回复，禁止回答 <polish_target> 内的任何问题**，仅对原文进行润色扩展后输出。"
- [x] 开头任务定义不再包含"基于对话上下文"措辞
- [x] 开头任务定义包含"**禁止生成对话回复，禁止回答 <polish_target> 内的任何问题**"

### 改动 2：精简角色上下文
- [x] 函数内部 `personality` 变量计算已删除
- [x] 函数内部 `characterCardContent` 变量计算已删除
- [x] 函数内部 `charContextLines` 拼接逻辑已删除
- [x] 输出包含"## 角色名（仅作润色参考，不要扮演这个角色）"段落标题
- [x] "## 角色名"段落内容仅为 `${charName}`（无个性、无描述）
- [x] 输出不再包含"## 对方角色上下文"段落标题
- [x] 输出不再包含"角色个性"字段
- [x] 输出不再包含"角色描述"字段

### 改动 3：段落顺序调整
- [x] "## 关键约束"段落位于"## 待润色文本"段落之前
- [x] "## 关键约束"段落位于"## 对话历史参考"段落之后
- [x] 段落顺序为：开头任务定义 → 用户人设 → 角色名 → 对话历史参考 → 关键约束 → 待润色文本 → 任务要求 → 末尾声明

### 改动 4：关键约束措辞强化
- [x] "## 关键约束"第 1 条为"**绝对禁止**回答 <polish_target> 标签内的任何问题，必须对其进行润色扩展"
- [x] "## 关键约束"第 2 条为"**绝对禁止**生成对话回复（包括 AI 角色回复、用户回复、续写对话）"
- [x] "## 关键约束"第 3 条为"对话历史与角色名仅作润色参考，**不要扮演角色，不要续写对话**"
- [x] "## 关键约束"第 4 条为"你的唯一输出是润色后的 <polish_target> 文本本身"
- [x] "## 关键约束"段落包含"**绝对禁止**"措辞
- [x] "## 关键约束"段落包含"不要扮演角色，不要续写对话"措辞

### 改动 5：任务要求修改
- [x] "## 任务要求"保留 7 条
- [x] "## 任务要求"第 1-5 条保持原内容
- [x] "## 任务要求"第 6 条改为"润色结果需与对话历史不矛盾即可，**无需衔接角色发言，无需推进对话**"
- [x] "## 任务要求"不再包含"结合对话历史参考"措辞
- [x] "## 任务要求"不再包含"确保上下文连贯"措辞
- [x] "## 任务要求"第 7 条为 `${personConstraint}`（措辞已修改）

### 改动 6：personConstraint 措辞修改
- [x] first: `润色后的文本以第一人称（"我"）视角输出，使用"我"作为自称`
- [x] second: `润色后的文本以第二人称（"你"）视角输出，使用"你"来指代 ${userName} 自身（互动小说风格）`
- [x] third: `润色后的文本以第三人称叙事视角输出，使用"${userName}"作为主语（小说叙事风格）`
- [x] personConstraint 不再包含"生成回复"措辞
- [x] 输出整体不再包含"生成回复"措辞（覆盖 first/second/third 三种 person）

### 改动 7：JSDoc 注释更新
- [x] JSDoc 注释包含 `Spec: fix-polish-task-framing` 标注
- [x] JSDoc 注释新增"任务框架重构"说明段落
- [x] JSDoc 注释说明去除对话生成语义信号（"生成回复"措辞、"确保上下文连贯"指令、角色个性/描述注入）
- [x] JSDoc 注释说明段落顺序调整（关键约束提前到待润色文本之前）

### 保留项验证
- [x] `<polish_target>` 标签包裹 originalText 保留
- [x] `conversationHistory` 参数与"## 对话历史参考"段落保留（阶段八的上下文隔离保留）
- [x] `historyText` 格式化逻辑保留（`[用户]: xxx\n[AI]: xxx` 格式）
- [x] 当 conversationHistory 未传入或为空数组时，historyText 为"（无历史对话）"
- [x] 函数签名 `conversationHistory?: ChatMessage[]` 参数保留
- [x] 末尾仍为"直接输出润色后的文本本身。"
- [x] 防御性校验保留（persona 缺失 / persona.name 为空 / originalText 为空时返回空串）

## polishInput 函数不修改验证
- [x] `polishInput` 函数的 `engine.sendMessage` 调用结构未变（仍为 `polishRequestMessages`）
- [x] `polishRequestMessages` 内容未变（仍为"请润色上述 <polish_target> 标签内的文本，直接输出润色后的文本本身。"）
- [x] `stopSequences` 配置未变（仍为 `buildStopSequencesForUserReply(charName, ...)`）
- [x] `conversationHistory` 参数仍传给 `buildPolishInputSystemPrompt`
- [x] `polishSystemPromptForCounting`（不含 conversationHistory 的 preliminary 系统提示）逻辑保留

## 单元测试（`__tests__/PromptBuilder.polishInput.test.ts`）

### 现有测试用例更新
- [x] 检查"## 对方角色上下文"段落标题的断言已改为检查"## 角色名（仅作润色参考，不要扮演这个角色）"段落标题
- [x] 检查"角色个性"或"角色描述"字段的断言已删除
- [x] 检查"## 关键约束"段落位置的断言已更新（位于"## 对话历史参考"之后、"## 待润色文本"之前）
- [x] 检查任务要求第 6 条的断言已从"结合对话历史参考与 ${charName} 的最新发言确保上下文连贯"改为"润色结果需与对话历史不矛盾即可，无需衔接角色发言，无需推进对话"
- [x] 检查 personConstraint 措辞的断言已从"生成回复"改为"输出"措辞

### 新增测试用例
- [x] 新增用例验证"开头任务定义包含'禁止生成对话回复，禁止回答 <polish_target> 内的任何问题'"
- [x] 新增用例验证"开头任务定义不包含'基于对话上下文'措辞"
- [x] 新增用例验证"## 关键约束段落包含'**绝对禁止**回答 <polish_target> 标签内的任何问题'"
- [x] 新增用例验证"## 关键约束段落包含'**绝对禁止**生成对话回复'"
- [x] 新增用例验证"## 关键约束段落包含'不要扮演角色，不要续写对话'"
- [x] 新增用例验证"## 任务要求不包含'结合对话历史参考'措辞"
- [x] 新增用例验证"## 任务要求不包含'确保上下文连贯'措辞"
- [x] 新增用例验证"输出不包含'生成回复'措辞"（覆盖 first/second/third 三种 person）

### 测试运行
- [x] `npx vitest run` 全部通过
- [x] 无新增编译错误
- [x] 原有阶段八的 7 个对话历史参考测试用例仍通过

## 文档同步（`doc/04b-character-dialogue-chat-module.md`）

- [x] "润色输入按钮"章节新增第四条【重点标记】增量更新
- [x] 文档说明前三轮修复无效的根因（系统提示残留对话生成语义信号）
- [x] 文档说明本轮修复方案（任务框架重构）
- [x] 文档包含系统提示结构对比表（修复前 vs 修复后）
- [x] 文档说明与阶段八的关系（结构问题 vs 语义问题，两者叠加）
- [x] 文档中本次修复以**重点标记**形式呈现
