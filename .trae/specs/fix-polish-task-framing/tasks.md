# Tasks

## 重构 buildPolishInputSystemPrompt 函数

- [x] Task 1: 重构 `buildPolishInputSystemPrompt` 函数（`PromptBuilder.ts:405-499`），实现七项改动
  - [x] SubTask 1.1: 修改开头任务定义，从"你是文本润色器，需要基于对话上下文优化用户 **${userName}** 的草稿文本。"改为"你是文本润色器，需要优化用户 **${userName}** 的草稿文本。**禁止生成对话回复，禁止回答 <polish_target> 内的任何问题**，仅对原文进行润色扩展后输出。"
  - [x] SubTask 1.2: 删除函数内部 `personality` 变量计算（约第 426-428 行 `const personality = ...`）—— 不再注入系统提示
  - [x] SubTask 1.3: 删除函数内部 `characterCardContent` 变量计算（约第 430-435 行 `let characterCardContent = ''...`）—— 不再注入系统提示
  - [x] SubTask 1.4: 删除函数内部 `charContextLines` 拼接逻辑（约第 437-444 行 `let charContextLines = ...`）—— 替换为单一 `${charName}`
  - [x] SubTask 1.5: 修改 personConstraint 措辞（约第 448-456 行）：
    - first: 从 `以第一人称（"我"）视角生成回复，使用"我"作为自称` 改为 `润色后的文本以第一人称（"我"）视角输出，使用"我"作为自称`
    - second: 从 `以第二人称（"你"）视角生成回复，使用"你"来指代 ${userName} 自身（互动小说风格）` 改为 `润色后的文本以第二人称（"你"）视角输出，使用"你"来指代 ${userName} 自身（互动小说风格）`
    - third: 从 `以第三人称叙事视角生成回复，使用"${userName}"作为主语（小说叙事风格）` 改为 `润色后的文本以第三人称叙事视角输出，使用"${userName}"作为主语（小说叙事风格）`
  - [x] SubTask 1.6: 重构返回模板字符串（约第 466-498 行），按新顺序组织段落：
    1. 开头任务定义（含禁止对话回复声明）
    2. `## 用户人设` 段落（保留原内容）
    3. `## 角色名（仅作润色参考，不要扮演这个角色）` 段落（仅 `${charName}`，替代原 `## 对方角色上下文` 段落）
    4. `## 对话历史参考（仅作上下文参考，不是润色对象，不要回答其中任何内容）` 段落（保留原 historyText 逻辑）
    5. `## 关键约束` 段落（**提前到 `## 待润色文本` 之前**），4 条约束改为强禁止措辞：
       - `**绝对禁止**回答 <polish_target> 标签内的任何问题，必须对其进行润色扩展`
       - `**绝对禁止**生成对话回复（包括 AI 角色回复、用户回复、续写对话）`
       - `对话历史与角色名仅作润色参考，**不要扮演角色，不要续写对话**`
       - `你的唯一输出是润色后的 <polish_target> 文本本身`
    6. `## 待润色文本` 段落（保留 `<polish_target>` 标签包裹 originalText）
    7. `## 任务要求` 段落，7 条改为：
       1. 保持用户原始意图与核心信息不变
       2. 提升表达精准度与场景适配度
       3. 符合 ${userName} 的人设特征与说话方式
       4. 仅输出润色后的文本，不要解释、不要引号包裹、不要前缀（如"${userName}:"）
       5. 润色后长度不应大幅偏离原文（建议 ±50% 以内）
       6. 润色结果需与对话历史不矛盾即可，**无需衔接角色发言，无需推进对话**
       7. ${personConstraint}
    8. 末尾保留 `直接输出润色后的文本本身。`
  - [x] SubTask 1.7: 更新 JSDoc 注释，在 `**润色上下文隔离**` 段落后追加 `**任务框架重构**` 说明：
    - 标注 `Spec: fix-polish-task-framing`
    - 说明去除对话生成语义信号（"生成回复"措辞、"确保上下文连贯"指令、角色个性/描述注入）
    - 说明段落顺序调整（关键约束提前到待润色文本之前）

## 更新单元测试

- [x] Task 2: 更新 `__tests__/PromptBuilder.polishInput.test.ts` 以反映新结构
  - [x] SubTask 2.1: 更新现有测试用例中检查"## 对方角色上下文"段落标题的断言，改为检查"## 角色名（仅作润色参考，不要扮演这个角色）"段落标题
  - [x] SubTask 2.2: 更新现有测试用例中检查"角色个性"或"角色描述"字段的断言，删除这些断言（这些字段不再注入）
  - [x] SubTask 2.3: 更新现有测试用例中检查"## 关键约束"段落位置的断言，确认其位于"## 对话历史参考"之后、"## 待润色文本"之前
  - [x] SubTask 2.4: 更新现有测试用例中检查任务要求第 6 条的断言，从"结合对话历史参考与 ${charName} 的最新发言确保上下文连贯"改为"润色结果需与对话历史不矛盾即可，无需衔接角色发言，无需推进对话"
  - [x] SubTask 2.5: 更新现有测试用例中检查 personConstraint 措辞的断言，从"生成回复"改为"输出"措辞
  - [x] SubTask 2.6: 新增测试用例验证"开头任务定义包含'禁止生成对话回复，禁止回答 <polish_target> 内的任何问题'"
  - [x] SubTask 2.7: 新增测试用例验证"开头任务定义不包含'基于对话上下文'措辞"
  - [x] SubTask 2.8: 新增测试用例验证"## 关键约束段落包含'**绝对禁止**回答 <polish_target> 标签内的任何问题'"
  - [x] SubTask 2.9: 新增测试用例验证"## 关键约束段落包含'**绝对禁止**生成对话回复'"
  - [x] SubTask 2.10: 新增测试用例验证"## 关键约束段落包含'不要扮演角色，不要续写对话'"
  - [x] SubTask 2.11: 新增测试用例验证"## 任务要求不包含'结合对话历史参考'措辞"
  - [x] SubTask 2.12: 新增测试用例验证"## 任务要求不包含'确保上下文连贯'措辞"
  - [x] SubTask 2.13: 新增测试用例验证"输出不包含'生成回复'措辞"（覆盖 first/second/third 三种 person）
  - [x] SubTask 2.14: 运行 `npx vitest run` 确认全部测试通过，无新增编译错误

## 文档同步

- [x] Task 3: 更新技术文档 `doc/04b-character-dialogue-chat-module.md`
  - [x] SubTask 3.1: 在"润色输入按钮"章节中新增第四条【重点标记】增量更新，说明：
    - 前三轮修复无效的根因：系统提示残留对话生成语义信号（"生成回复"措辞、"确保上下文连贯"指令、角色个性/描述注入、关键约束位置在待润色文本之后）
    - 本轮修复方案：任务框架重构——强化开头任务定义、精简角色上下文为角色名、关键约束提前到待润色文本之前、关键约束使用强禁止措辞、任务要求第 6 条改为反对话生成、personConstraint 措辞从"生成回复"改为"输出"
    - 与阶段八的关系：阶段八解决"消息结构"问题（对话历史以 assistant 结尾触发续写本能），本 spec 解决"系统提示语义"问题（残留对话生成关键词），两者叠加才能彻底切断 AI 走对话生成路径
    - 系统提示结构对比表（修复前 vs 修复后）
  - [x] SubTask 3.2: 在文档中标注本次修复为 **重点标记**（用户报告的顽固 bug 修复，前三轮修复无效），便于后续追溯

# Task Dependencies

- Task 1 与 Task 2 有依赖关系：Task 2 依赖 Task 1（测试用例需基于新函数输出结构）
- 建议 Task 1 先实施，Task 2 后实施（或同一 agent 顺序实施两任务）
- Task 3 依赖 Task 1、Task 2
