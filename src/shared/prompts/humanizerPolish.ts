/**
 * 润色/生成功能"去 AI 味"规则模块 — humanizerPolish
 *
 * Spec: polish-deai-humanizer（2026-08-21 第二次返工：蒸馏版强度不足，升级为完整版）
 *
 * 来源（两层，分层标注）：
 * 1. humanizer-zh-enhanced 技能包 references/humanizer-guide.md（430 行 / 27 种模式）
 *    —— 完整移植（含全部词表与改写示例），不再蒸馏简写
 * 2. RP 域适配层（指南没有的内容，自创 + 用户实测反馈）：
 *    - "禁忌之美""冰冷的""嘴角勾起一抹"等中文 RP 高频 AI 腔词表（指南 27 模式不覆盖）
 *    - 生成场景的 JSON-aware 声明与设定集文体要求
 *
 * 迭代史（防止再次退化，勿删）：
 * - v1 蒸馏 ~250 字/8 概念 → 用户对照原文指出覆盖不全（漏掉宣传腔/意义拔高等同构模式）
 * - v2 蒸馏 ~500 字/10 条 → 用户实测"冰冷的"反复出现（该词不在指南词表，蒸馏必然漏）
 * - v3（当前）完整移植指南 + RP 域补充词表。教训：规则列举式提示词只对明确列出的词有效，
 *   覆盖度靠"完整词表"而非"精炼概括"
 *
 * 设计要点：
 * 1. 运行时注入而非烘焙进模板——开关关闭时必须能完全撤下规则；
 *    且存量 DB 模板不更新（mergeNewDefaultTemplates 已知坑），运行时注入对全体用户一致生效
 * 2. 警示表是"换具体表达"的引导而非机械禁词（humanizer 核心理念：
 *    用具体的动作、感官、事实替代抽象浮夸）
 * 3. 用户要求优先——例外条款确保用户明确要求"华丽古风"等风格时不被阻拦
 */

/** 规则块锚点（防重复注入的守卫标记） */
export const HUMANIZER_POLISH_ANCHOR = '【去AI味约束】';

/** RP 域高频 AI 腔词表（指南 27 模式不覆盖，用户实测 + SillyTavern 社区公认高频） */
export const HUMANIZER_RP_WARNLIST = `【RP文本高频AI腔——发现即改写】
"冰冷的"（冰冷的语气/金属/现实）、"淡淡的"（淡淡地说/淡淡一笑）、"一丝"（一丝不易察觉的…）、"某种"（某种情绪/某种说不清的东西）、"深邃的"（深邃的眼眸）、"锐利的"（锐利的目光）、"无声的"（无声的威胁）、"禁忌之美""不可言说""命运的丝线""缱绻""旖旎"、"嘴角勾起一抹"（弧度/笑意）、"眼底闪过一丝"、"眼底暗了暗"、"睫毛颤了颤"、"心脏漏跳一拍"、"空气仿佛凝固"、"空气骤然安静"、"时间仿佛静止"、"温度仿佛…"、"声音里带着一丝…"
以上词条出现时，换成具体可观察的动作、感官细节或事实。例如"冰冷的语气"→写他具体说了什么话、做了什么动作让人感到疏远。`;

/**
 * humanizer-zh-enhanced 完整指南移植（27 种模式全量）。
 *
 * 来源对照：references/humanizer-guide.md 全文，仅做结构压缩（去装饰分隔线），
 * 词表与改写示例完整保留。token 成本 ~7k（用户明确要求完整接入，勿再精简）。
 */
export const HUMANIZER_FULL_GUIDE = `【去AI味完整指南——27种AI写作模式识别与改写】

■ 核心规则（处理文本时牢记5条）
1. 删除填充短语——去除开场白和强调性拐杖词
2. 打破公式结构——避免二元对比、戏剧性分段、修辞性设置
3. 变化节奏——混合句子长度。两项优于三项。段落结尾要多样化
4. 信任读者——直接陈述事实，跳过软化、辩解和手把手引导
5. 删除金句——如果听起来像可引用的语句，重写它

■ 增加人味（有真实的人在场）
- 有观点：不只报告事实——做出反应。"我真的不知道怎么看待这事"比中立列利弊更有人味
- 承认复杂性："这令人印象深刻但也有点不安"胜过"这令人印象深刻"
- 允许一些混乱：跑题、题外话和半成型的想法是人性体现
- 对感受要具体：不是"这令人担忧"，而是"凌晨三点智能体还在不停地运转，这让人不安"

■ 27种AI写作模式（词表 + 改写示例）

#1 过度强调意义、遗产和更广泛趋势
词汇：作为/充当、标志着、见证了、是……的体现/证明/提醒、至关重要的/核心的/关键性的作用、凸显/强调/彰显了其重要性、反映了更广泛的、象征着其持续的/永恒的/持久的、为……做出贡献、为……奠定基础、代表/标志着一个转变、关键转折点、不断演变的格局、不可磨灭的印记、深深植根于、具有重要的理论意义和现实意义
例：✗"统计局于1989年正式成立，标志着西班牙区域统计演变史上的关键时刻，是更广泛运动的一部分，旨在分散行政职能。" → ✓"统计局成立于1989年，负责独立收集和发布区域统计数据。"

#2 过度强调知名度和媒体报道
词汇：独立报道、地方/区域/国家媒体、由知名专家撰写、被多家权威媒体报道
例：✗"她的观点被《纽约时报》、BBC引用，拥有超过50万粉丝。" → ✓"在2024年《纽约时报》的采访中，她认为AI监管应关注结果。"

#3 句尾虚假深度（补充短语）
词汇：突出/强调/彰显……、确保……、反映/象征……、为……做出贡献、培养/促进……、展示……、从而……、进而……、以此……
例：✗"寺庙的蓝色、绿色和金色色调与自然美景产生共鸣，反映了社区与土地的深厚联系。" → ✓"寺庙使用蓝、绿、金三种颜色。建筑师说这些颜色来自当地的蓝帽花和海湾海岸。"

#4 宣传和广告式语言
词汇：拥有（夸张）、充满活力的、丰富的（比喻）、深刻的、增强其、展示、体现、致力于、自然之美、坐落于、位于……的中心、开创性的、著名的、令人叹为观止的、必游之地、迷人的、风景如画
例：✗"坐落在令人叹为观止的区域内，是一座充满活力的城镇，拥有丰富的文化遗产和迷人的自然美景。" → ✓"该镇以每周集市和18世纪教堂闻名。"

#5 模糊归因和含糊措辞
词汇：行业报告显示、观察者指出、专家认为/指出/表示、一些批评者认为、多个来源、相关研究指出、文献表明、据报道、业内人士普遍认为
例：✗"专家认为它在区域生态系统中发挥着至关重要的作用。" → ✓"根据中科院2019年的调查，该河支持多种特有鱼类。"

#6 提纲式"挑战与未来展望"
词汇：尽管其……面临若干挑战、尽管存在这些挑战、挑战与遗产、未来展望、前景与展望、问题与对策
例：✗"尽管面临挑战，凭借其战略位置，该镇继续蓬勃发展。" → ✓"2015年三个新IT园区开业后，交通拥堵加剧。市政公司2022年启动了排水项目。"

#7 过度使用的"AI词汇"
词汇：此外、与……保持一致、至关重要、深入探讨、强调、持久的、增强、培养、获得、突出（动词）、相互作用、复杂/复杂性、关键（形容词）、格局（抽象名词）、展示、织锦（抽象名词）、证明、宝贵的、充满活力的、深入剖析、进一步分析、显而易见、不争的事实、毋庸置疑
例：✗"此外，菜肴的显著特征是加入骆驼肉。意大利殖民影响的持久证明是当地烹饪格局中广泛采用意大利面。" → ✓"菜肴还包括骆驼肉，被视为美味。意大利面仍很常见，尤其在南部。"

#8 回避"是"（系动词回避）
词汇：作为/成为/充当[一个]、设有/拥有/具备/提供[一个]、隶属于、归属于、构成
例：✗"画廊作为当代艺术展览空间。设有四个独立空间，拥有超过3000平方英尺。" → ✓"画廊是当代艺术展览空间，有四个房间，总面积3000平方英尺。"

#9 否定式排比
例：✗"这不仅仅是节拍在人声下流动；它是攻击性和氛围的一部分。这不仅仅是一首歌，而是一种声明。" → ✓"沉重的节拍增加了攻击性的基调。"

#10 三段式法则过度使用
例：✗"活动包括主题演讲、小组讨论和社交机会。与会者可以期待创新、灵感和行业洞察。" → ✓"活动包括演讲和小组讨论。会议之间还有非正式社交的时间。"

#11 刻意换词（同义词循环）
例：✗"主人公面临许多挑战。主要角色必须克服障碍。中心人物最终获得胜利。英雄回到家中。" → ✓"主人公面临许多挑战，但最终获胜回到家中。"

#12 虚假范围
例：✗"我们从大爆炸的奇点带到宏伟的宇宙网，从恒星的诞生和死亡到暗物质的神秘舞蹈。" → ✓"这本书涵盖大爆炸、恒星形成和当前关于暗物质的理论。"

#13 被动语态和无主语碎片句
例：✗"无需进行任何手动配置。数据将被自动保存到服务器。" → ✓"你不需要手动配置。系统会自动把数据保存到服务器。"

#14 破折号过度使用
例：✗"这个术语主要由机构推广——而不是由人民自己——但这种错误标记仍在继续。" → ✓"这个术语主要由机构推广，而不是由人民自己，但这种错误标记仍在继续。"

#15 粗体过度使用
例：✗"它融合了**OKR**、**KPI**和**商业模式画布**。" → ✓"它融合了OKR、KPI和商业模式画布。"

#16 内联标题垂直列表
例：✗"- **用户体验：** 通过新界面得到显著改善。- **性能：** 通过优化算法得到增强。" → ✓"更新改进了界面，加快了加载时间，并添加了端到端加密。"

#17 标题大小写（中英文混合标题的大小写一致性）
例：✗"## Strategic Negotiations And Global Partnerships" → ✓"## Strategic negotiations and global partnerships"

#18 表情符号
例：✗"🚀 **启动阶段：** 产品在第三季度发布" → ✓"产品在第三季度发布。"

#19 弯引号（中文使用「」或""，不要用弯引号""）

#20 连字符配对词滥用
词汇：端到端、跨部门、面向客户、数据驱动、决策制定、众所周知、高质量、实时、长期、全链路、闭环、赋能、颗粒度、底层逻辑

#21 说服权威套路
词汇：真正的问题是、其核心在于、事实上、真正重要的是、从根本上说、更深层的问题是、问题的本质是
例：✗"真正的问题是团队能否适应变化。其核心在于，真正重要的是组织的准备程度。" → ✓"问题在于团队能不能适应变化。这主要取决于组织是否准备好改变习惯。"

#22 协作交流痕迹
词汇：希望这对您有帮助、当然！、您说得完全正确！、请告诉我、如果您有任何问题、欢迎随时联系

#23 知识截止日期免责声明
词汇：截至[日期]、根据我最后的训练更新、基于可用信息、据我所知、在我的知识范围之内

#24 谄媚语气
例：✗"好问题！您说得完全正确，这是一个复杂的话题。" → ✓"您提到的经济因素确实相关。"

#25 填充短语
"为了实现这一目标"→"为了这个"；"由于下雨的事实"→"因为下雨"；"在这个时间点"→"现在"；"系统具有处理的能力"→"系统可以处理"；"值得注意的是数据显示"→"数据显示"；"不可否认的是/不得不指出的是/综上所述/众所周知/毋庸置疑"→整句删掉

#26 过度限定
例：✗"可以潜在地可能被认为该政策可能会对结果产生一些影响。" → ✓"该政策可能会影响结果。"

#27 通用积极结论
例：✗"公司的未来看起来光明。激动人心的时代即将到来。" → ✓"该公司计划明年再增加两个分公司。"

■ 交付前快速检查清单
- 连续三个句子长度相同？打断一个
- 段落以简洁单行结尾？变换结尾方式
- 揭示前有破折号？删除它
- 解释了隐喻？相信读者
- 用了"此外""然而"等连接词？考虑删除
- 三段式列举？改为两项
- "不仅…而且…"结构？重写
- "从X到Y"虚假范围？直接列出
- 开头有"值得注意的是/不可否认/众所周知"？删掉
- 有重复的同义词换词？合二为一

■ 最终反AI自检
完成改写后自问："这段文字哪里还显得是AI写的？"凡读起来太工整、太像范文的句子，再改一次。`;

/** 润色场景完整规则块（完整指南 + RP 域词表 + 例外条款） */
export const HUMANIZER_POLISH_RULES = `\n${HUMANIZER_POLISH_ANCHOR}（除非用户的润色要求明确指定了其他风格，否则必须遵守。规则很多，逐条对照检查）
${HUMANIZER_RP_WARNLIST}

${HUMANIZER_FULL_GUIDE}`;

/**
 * 按开关状态为润色 system prompt 注入去 AI 味规则块。
 *
 * @param systemPrompt 模板系统产出的润色指令
 * @param enabled 去AI味开关（默认路径为 true）
 * @returns 处理后的 system prompt
 */
export function withHumanizerRules(systemPrompt: string, enabled: boolean): string {
  // 开关关闭：原样返回（用户自定义模板自带锚点属用户自身选择，不受开关影响）
  if (!enabled) return systemPrompt;
  // 空输入保护
  if (!systemPrompt) return systemPrompt;
  // 锚点守卫：已含规则块（用户模板手动添加过）不重复注入
  if (systemPrompt.includes(HUMANIZER_POLISH_ANCHOR)) return systemPrompt;

  return systemPrompt.trimEnd() + '\n' + HUMANIZER_POLISH_RULES;
}

// ==================== 生成场景变体（Spec: polish-deai-humanizer 扩展 / 世界书条目生成） ====================

/** 生成场景规则块锚点（独立于润色锚点，防重复注入） */
export const HUMANIZER_GENERATION_ANCHOR = '【文本风格约束】';

/**
 * 生成场景规则块（JSON-aware + 完整指南）。
 *
 * 与润色规则块的关键差异：
 * 1. 生成模板强制 JSON-only 输出（"只返回 JSON，首字符必须是 {"），
 *    规则块必须显式声明"仅约束 JSON 内文本字段的行文，不改变 JSON 结构与输出格式"，
 *    否则模型可能被带偏输出散文、破坏 JSON 解析
 * 2. 世界书条目是设定集文体（供 RP 检索注入），不是散文——
 *    风格目标调整为"像百科条目/设定集那样平实陈述"（指南#22-24 对话类模式天然不适用）
 */
export const HUMANIZER_GENERATION_RULES = `\n${HUMANIZER_GENERATION_ANCHOR}（以下仅约束 JSON 中 name/content/description/comment 等文本字段的行文，不改变 JSON 结构与"只返回 JSON"的输出格式）
【设定集文体总则】像设定集/百科条目那样平实陈述：直接写"是什么、有什么规则、发生过什么"，每句携带具体信息（名称、数字、规则、关系、事件），不搞散文式抒情。
${HUMANIZER_RP_WARNLIST}

${HUMANIZER_FULL_GUIDE}`;

/**
 * 为世界书条目生成 system prompt 注入去 AI 味规则块。
 *
 * 用户确认策略：默认开启、无开关（生成入口分散在 5+ 个 Modal 且智能体流程无 UI，
 * 逐 Modal 加开关属 UI 膨胀；用户自定义模板可手动加锚点自主控制）。
 *
 * @param systemPrompt 生成模板产出的 system prompt
 * @returns 处理后的 system prompt
 */
export function withHumanizerGenerationRules(systemPrompt: string): string {
  // 空输入保护
  if (!systemPrompt) return systemPrompt;
  // 锚点守卫：模板已含规则块（用户手动添加过）不重复注入
  if (systemPrompt.includes(HUMANIZER_GENERATION_ANCHOR)) return systemPrompt;

  return systemPrompt.trimEnd() + '\n' + HUMANIZER_GENERATION_RULES;
}

// ==================== 文本生成场景（角色卡字段等非 JSON 直接文本输出） ====================

/**
 * 文本生成规则块。
 *
 * 与 JSON-aware 生成变体（HUMANIZER_GENERATION_RULES）的差异：
 * 1. **无 JSON 声明**——角色卡字段生成（character-card.generate 模板）是直接文本输出，
 *    若注入"仅约束 JSON 内字段/不改变 JSON 结构"的声明，会误导模型输出 JSON、破坏模板格式
 * 2. 文体总则适配 RP 域：角色卡字段既有设定描述（description/personality 等，要求平实具体）
 *    也有对话文本（first_mes/alternate_greetings 等，要求像真人说话）——
 *    不能像世界书那样一律"百科条目化"
 */
export const HUMANIZER_TEXTGEN_RULES = `\n${HUMANIZER_GENERATION_ANCHOR}（以下约束本次生成文本的行文风格。设定类字段平实陈述、每句携带具体信息；对话/开场白/问候语要像真人说话——句式自然、不堆砌辞藻）
${HUMANIZER_RP_WARNLIST}

${HUMANIZER_FULL_GUIDE}`;

/**
 * 为直接文本输出型生成的 system prompt 注入去 AI 味规则块
 * （角色卡字段生成等非 JSON 场景）。
 *
 * 启用策略与世界书生成一致：默认开启、无开关。
 *
 * @param systemPrompt 生成模板产出的 system prompt
 * @returns 处理后的 system prompt
 */
export function withHumanizerTextgenRules(systemPrompt: string): string {
  // 空输入保护
  if (!systemPrompt) return systemPrompt;
  // 锚点守卫：与生成变体共用锚点（两者不会同时注入同一请求，共享守卫无冲突）
  if (systemPrompt.includes(HUMANIZER_GENERATION_ANCHOR)) return systemPrompt;

  return systemPrompt.trimEnd() + '\n' + HUMANIZER_TEXTGEN_RULES;
}
