# AGENTS.md

项目级 AI 代理指导文件。此文件中的规则在所有会话中持续生效。

## 用户偏好

### 子代理（Subagent）使用规则

- **禁止自动启动子代理**：不要主动使用 Task 工具启动子代理（subagent）来处理任务。
- 所有任务应在主对话中直接完成。
- 仅当用户明确要求使用子代理时，才可以使用 Task 工具。
- 原因：子代理消耗额外 token，且用户无法直接观察中间过程。

### 开发服务器自动重启规则

- **修改代码后必须自动确保变更生效**：若 HMR 未生效或需要重新编译，必须自动重启开发服务器，不得要求用户手动操作。
- 流程：查找 Vite/node 进程（`Get-CimInstance Win32_Process`）→ `Stop-Process` 终止 → `npm run dev` 后台重启。
- 仅在自动重启无效时才请用户介入。
- 原因：用户期望 AI 全权负责代码修改到生效的完整闭环，手动编译/重启是 AI 的失职。

---

*此文件由 self-improvement 技能维护。学习条目记录在 `.learnings/LEARNINGS.md` 中。*
